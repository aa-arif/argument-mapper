# ---------------------------------------------------------------------------
# Corpora. Never commit these.
#
# Argument Annotated Essays v2 is licensed under a TU Darmstadt / UKP agreement
# whose clause 2.2 forbids publishing, redistributing, or displaying the data in
# any form. This repository is public. arg-microtexts is CC BY-NC-SA 4.0.
# A CI job re-checks this; see .github/workflows/ci.yml.
# ---------------------------------------------------------------------------
# Anchored to the repo root so it does not also match src/argmap/data/
/data/
*.ann
brat-project-final*
ArgumentAnnotatedEssays*
arg-microtexts*

# Generated few-shot exemplars contain corpus excerpts
prompts/exemplars*.json

# Model weights and adapters (plausibly derivative of the corpus)
*.safetensors
*.bin
*.gguf
checkpoints/
adapters/

# ---------------------------------------------------------------------------
# Working documents stay local: PLAN, PROGRESS, DECISIONS, GUIDE and
# RESUME_BULLETS. README.md is the exception - it is the repository's front
# door and is committed. Measured results are written as .json/.csv under
# results/, not markdown.
# ---------------------------------------------------------------------------
*.md
!README.md

# ---------------------------------------------------------------------------
# Caches
# ---------------------------------------------------------------------------
.cache/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.pyright/
htmlcov/
.coverage
*.egg-info/
dist/
build/

# ---------------------------------------------------------------------------
# Environments
# ---------------------------------------------------------------------------
.venv/
venv/
.env
.env.local

# ---------------------------------------------------------------------------
# Node / frontend
# ---------------------------------------------------------------------------
node_modules/
web/dist/
web/dist-ssr/
*.local

# ---------------------------------------------------------------------------
# Editors / OS
# ---------------------------------------------------------------------------
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.swp
